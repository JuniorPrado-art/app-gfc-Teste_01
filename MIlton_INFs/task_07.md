# Tarefas para Implementação "Caixas Sem Gravação"

- `[/]` 1. **Backend (`app.py`)**: Adicionar a rota `/api/monitoramento/caixas_sem_gravacao` executando a query unida à tabela `empresa`.
- `[ ]` 2. **Frontend (`dashboard/page.tsx`)**: Adicionar a lógica de busca do count da nova rota e o novo card "Observações:" com as formatações exigidas.
- `[ ]` 3. **Frontend (`dashboard/layout.tsx`)**: Adicionar a seção "OBSERVAÇÕES" no menu lateral.
- `[ ]` 4. **Frontend (Novas Páginas)**:
  - `[ ]` 4.1. Criar `/dashboard/caixas-sem-gravacao/page.tsx` com a tabela (Empresa, Data, Turno, Usuario).
  - `[ ]` 4.2. Criar `/dashboard/descontos/page.tsx` ("Em construção").
  - `[ ]` 4.3. Criar `/dashboard/estoque/page.tsx` ("Em construção").
  - `[ ]` 4.4. Criar `/dashboard/exclusoes/page.tsx` ("Em construção").
- `[ ]` 5. **Git Commit**: Commitar as alterações.
